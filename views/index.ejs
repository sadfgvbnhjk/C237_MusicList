<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container"><br><br><br><br><br>
        <div class="text-center mt-5">
            <h1 class="mt-5">Welcome to MusicHub!</h1>
        </div>
        <% if (messages && messages.length > 0) { %>
            <div class="alert alert-success">
                <% messages.forEach(function(message) { %>
                    <p><%= message %></p>
                <% }); %>
            </div>
            Hello , <%= user.username %>
            <br> <a href="/logout" class="btn btn-primary mt-3">Logout</a>
        <% } else { %>
            <div class="row row-cols-1 row-cols-md-3 g-4 p-5 justify-content-center">
                <div class="card text-center">
                    <img class="card-img-top mx-auto d-block" src="" style="width: 100%;"> <!-- Register Image placeholder -->
                    <div class="card-body">
                        <h4 class="card-title">Register</h4>
                        <p class="card-text">Register an account to access MusicHub!</p>
                        <div class="divbtn">
                            <a href="/register"><button type="submit" class="btn btn-outline-dark btn-dark" style="color: white;">Register</button></a>
                        </div>
                    </div>
                </div>
                <div class="card text-center">
                    <img class="card-img-top mx-auto d-block" src="" style="width: 100%;"> <!-- Login Image placeholder -->
                    <div class="card-body">
                        <h4 class="card-title">Login</h4>
                        <p class="card-text">Already have an account? Login here!</p>
                        <div class="divbtn">
                            <a href="/login"><button type="submit" class="btn btn-outline-dark btn-dark" style="color: white;">Login</button></a>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>
