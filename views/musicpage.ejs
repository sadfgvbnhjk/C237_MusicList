<!DOCTYPE html>
<html lang="en">

<head>
    <title>Music Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <div class="container">
        <h1 class="mt-5">Music Page</h1>
        <% if (musicList && musicList.length > 0) %>
        <% musicList.forEach(function(music)) { %> <!-- Check if music data exists -->
            <div>
                <p><b>Music Title:</b><br>
                    <%= music_list.title %>
                </p>
                <p><b>Artist:</b><br>
                    <%= music_list.artist %>
                </p>
                <p><b>Genre:</b><br>
                    <%= music_list.genre %>
                </p>
                <p><b>Language:</b><br>
                    <%= music_list.language %>
                </p>
                <p><b>Music Image:</b><br>
                    <img src="<%= music_list.image %>" width="20%">
                </p>
                <p><b>Music Link:</b><br>
                    <%= music_list.link %>
                </p>
            </div>
            <% } else { %>
                <p>No music found.</p>
                <% } %>
                    <a href="/">Back</a>
    </div>

</body>

</html>
