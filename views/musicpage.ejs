<!DOCTYPE html>
<html lang="en">

<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>music page</title>
</head>

<body>

    <div class="container">
        <h1 class="mt-5">Music Page</h1>
        <% if (music_list && music_list.length> 0) { %>
            <% music_list.forEach(function(music) { %> <!-- Check if music data exists -->
                <div>
                    <p><b>Music Title:</b><br>
                        <%= music_list.title %>
                    </p>
                    <p><b>Artist:</b><br>
                        <%= music_list.artist %>
                    </p>
                    <p><b>Genre:</b><br>
                        <%= music_list.genre %>
                    </p>
                    <p><b>Language:</b><br>
                        <%= music_list.language %>
                    </p>
                    <p><b>Music Image:</b><br>
                        <img src="<%= music_list.image %>" width="20%">
                    </p>
                    <p><b>Music Link:</b><br>
                        <%= music_list.link %>
                    </p>
                </div>
            <% }); %>
        <% } else { %>
            <p>No music found.</p>
        <% } %>
        <a href="/">Back</a>
    </div>

</body>

</html>
